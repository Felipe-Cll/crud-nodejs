<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>CRUD in NodeJS</title>
    <link rel="stylesheet" type="text/css" href="/css/style.css">
</head>
<body>
    <div id="titulo">
        <h1>Candidatos</h1>
    </div>
    
    <section id="tableEfeito">
        <h2>Registros</h2>   
        <table id="tableInterno">
        <thead class="organizador">    
            <tr>
                <th scope="col" class="input-text-titulo">Nome</th>
                <th scope="col" class="input-text-titulo">Email</th>
                <th scope="col" class="input-text-titulo">Telefone</th>
                <th scope="col" class="input-text-titulo">Ação</th>
            </tr>
        </thead>
            <tbody class="organizador">
            <% data.forEach(function(details) { %>
                <tr>
                    <td class="input-text"><%= details.name %></td>
                    <td class="input-text"><%= details.email %></td>
                    <td class="input-text"><%= details.tel %></td>
                    <td class="input-text-B"><a href="/edit/<%= details._id %>" class="editando">Editar</a> - <a href="/delete/<%= details._id %>" class="deletando">Deletar</a></td>
                </tr>
                <% }) %>
            </tbody>
        </table>
            <button class="input-btn"><a href="/">Novo</a></button>
    </section>
</body>
</html>